# ============================================================
# COT Synthetic Dataset Generator — Dependencies
# ============================================================
#
# PRIMARY: Ollama backend (no Python package needed!)
#   1. Install Ollama: https://ollama.com
#   2. Pull models: ollama pull qwen3:8b
#   3. pip install -r requirements.txt
#   4. python run_pipeline.py
#
# FALLBACK: HF transformers (slower, if Ollama unavailable)
#   Uncomment the torch/transformers lines below.

# ── Core (always needed) ────────────────────────────────────
pyyaml>=6.0
jinja2>=3.1
pandas>=2.0
pyarrow>=14.0
tqdm>=4.66

# ── Validation & Quality ────────────────────────────────────
# No extra packages needed for Ollama-based validation.
# If using HF backends for Judge models, uncomment below.

# ── HF backend & Upload (optional) ──────────────────────────
huggingface_hub>=0.20
datasets>=2.18
# torch>=2.1
# transformers>=4.40
# accelerate>=0.28
# bitsandbytes>=0.43
